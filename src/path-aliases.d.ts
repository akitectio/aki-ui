/**
 * This file provides JSDoc path alias definitions to help with code completion
 * and navigation in editors like VS Code when using path aliases.
 *
 * @typedef {import('./lib/components').Button} Button
 * @typedef {import('./lib/components').Card} Card
 * @typedef {import('./lib/components').Alert} Alert
 * @typedef {import('./lib/components').Accordion} Accordion
 * @typedef {import('./lib/components').Badge} Badge
 * @typedef {import('./lib/components').Breadcrumb} Breadcrumb
 * @typedef {import('./lib/components').ButtonGroup} ButtonGroup
 * @typedef {import('./lib/components').Dropdown} Dropdown
 * @typedef {import('./lib/components').FormControl} FormControl
 * @typedef {import('./lib/components').Modal} Modal
 * @typedef {import('./lib/components').Pagination} Pagination
 *
 * @typedef {import('./lib/theme').AkiUIProvider} AkiUIProvider
 * @typedef {import('./lib/theme').ThemeProvider} ThemeProvider
 * @typedef {import('./lib/theme').ColorModeProvider} ColorModeProvider
 * @typedef {import('./lib/theme').DirectionProvider} DirectionProvider
 * @typedef {import('./lib/theme').ThemeContext} ThemeContext
 */

// This file is for documentation purposes only and doesn't export anything
export {};
